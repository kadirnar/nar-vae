[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nar-vae"
dynamic = ["version"]
description = "Low-cost non-autoregressive diffusion and flow-matching text-to-speech toolkit"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "NAR-VAE Contributors", email = "kadir.nar@hotmail.com" },
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
]
dependencies = [
    "torch>=2.2",
    "tiktoken",
    "einops",
    "numpy",
    "huggingface-hub",
    "tqdm",
    "soundfile",
    "torchaudio>=2.2",
    "descript-audiotools>=0.7.2",
    # Prevent legacy numba releases, which cannot install on supported Python versions.
    "numba>=0.60",
    "tomli>=2.0; python_version < '3.11'",
]

[project.urls]
Homepage = "https://github.com/kadirnar/nar-vae"
Repository = "https://github.com/kadirnar/nar-vae"
Issues = "https://github.com/kadirnar/nar-vae/issues"

[project.optional-dependencies]
train = [
    "accelerate>=1.3,<2",
    "datasets==3.4.1",
    "librosa",
    "PyYAML",
    "torchcodec",
    "transformers>=4.49,<5",
]
wandb = [
    "wandb",
]
turbo = [
    "cache-dit>=1.5,<2",
]
flash-attention = [
    "kernels>=0.16,<0.17",
]
dev = [
    "build",
    "ruff",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.dynamic]
version = { attr = "vyvotts._version.__version__" }

[tool.setuptools.packages.find]
include = ["nar_vae*", "vyvotts*"]

[tool.setuptools.package-data]
vyvotts = ["configs/*.yaml", "configs/*.toml"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.ruff.format]
indent-style = "space"
quote-style = "double"
